
<h1 class="mb-4"><%= board.title %></h1>
<p><%= board.content %></p>
<% if (board.image) { %>
    <img src="/uploads/<%= board.image %>" alt="게시물 이미지" class="mt-3" style="max-width: 100px;">
<% } %>
